"use client";

import { useRef, useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Heart, HeartHandshake, Sprout, Shield, CheckCircle, Users, Lock, Compass, Globe, Feather, Search, Sun } from "lucide-react";

/* ───── helpers ───── */

function Section({ id, bg, children, className = "" }: { id?: string; bg: string; children: React.ReactNode; className?: string }) {
  return (
    <section id={id} className={`px-4 py-10 sm:px-6 sm:py-14 md:py-16 ${className}`} style={{ background: bg }}>
      <div className="mx-auto max-w-6xl">{children}</div>
    </section>
  );
}

function FadeUp({ children, className = "" }: { children: React.ReactNode; className?: string }) {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const obs = new IntersectionObserver(
      ([entry]) => { if (entry.isIntersecting) { setVisible(true); obs.disconnect(); } },
      { rootMargin: "-80px" },
    );
    obs.observe(el);
    return () => obs.disconnect();
  }, []);
  return (
    <div
      ref={ref}
      className={className}
      style={{
        opacity: visible ? 1 : 0,
        transform: visible ? "translateY(0)" : "translateY(30px)",
        transition: "opacity 0.6s ease-out, transform 0.6s ease-out",
      }}
    >
      {children}
    </div>
  );
}

const SHADOW = "shadow-[0_8px_30px_rgba(236,103,27,0.08)]";

/* ───── Page ───── */

export default function Home() {
  const router = useRouter();

  return (
    <div className="bg-background font-body text-foreground">
        {/* ═══════ Section 1: Hero ═══════ */}
      <div
        className="relative flex min-h-dvh flex-col items-center justify-center gap-8 px-6 py-14 lg:flex-row lg:gap-16 lg:px-20 lg:py-20"
        style={{ background: "#FDFBF9" }}
      >
        {/* ─── Text ─── */}
        <div className="relative z-10 w-full max-w-xl text-center">
          <h1
            className="font-name leading-tight"
            style={{
              fontSize: "clamp(32px, 5vw, 48px)",
              fontWeight: 600,
              color: "#2D2A24",
            }}
          >
            Where Spiritual Seekers Find Their Life Partner
          </h1>

          <p className="mt-4 text-lg font-medium md:text-xl" style={{ color: "#6B655A" }}>
            You&apos;ve found your path. Now find the one to walk it with.
          </p>

          <p className="mt-6 max-w-lg leading-relaxed" style={{ fontSize: "16px", color: "#2D2A24" }}>
            A conscious matchmaking platform for seekers from Isha, ISKCON, Osho,
            Art of Living, and all spiritual paths. Because the journey within is
            beautifully shared together.
          </p>

          <div className="mt-8 flex items-center justify-center gap-4">
            <button
              type="button"
              onClick={() => router.push("/auth")}
              className="inline-flex items-center gap-2 rounded-full px-8 py-3.5 text-base font-semibold text-white shadow-lg transition-all hover:brightness-110 active:scale-[0.97]"
              style={{ background: "#EC671B", boxShadow: "0 4px 20px rgba(236,103,27,0.35)" }}
            >
              Find Your Life Partner <ArrowRight className="h-4 w-4" />
            </button>
          </div>
        </div>

        {/* ─── Photos ─── */}

        {/* Mobile: 3 compact photos at bottom (absolute, no layout) */}
        <div className="pointer-events-none absolute bottom-0 left-1/2 -translate-x-1/2 lg:hidden">
          <div className="relative" style={{ width: 240, height: 150 }}>
            {[
              { img: 36, rot: -6, offset: -55, y: 0 },
              { img: 38, rot: 0,  offset: 0,   y: 0 },
              { img: 40, rot: 6,  offset: 55,  y: 0 },
            ].map((p, i) => (
              <div key={p.img} className="absolute overflow-hidden rounded-xl border-[3px] border-white shadow-lg"
                style={{
                  width: 100, height: 140,
                  top: `${p.y}px`,
                  left: `calc(50% + ${p.offset}px)`,
                  transform: `translate(-50%, 0) rotate(${p.rot}deg)`,
                  zIndex: i,
                }}
              >
                <img src={`https://i.pravatar.cc/400?img=${p.img}`} alt="" className="h-full w-full object-cover" />
              </div>
            ))}
          </div>
        </div>

        {/* Desktop: 5 photos in normal flex flow */}
        <div className="hidden shrink-0 lg:flex lg:items-center">
          <div className="relative" style={{ width: 380, height: 530 }}>
            {[
              { img: 36, rot: -3, offset: -35, y: 0 },
              { img: 37, rot: -1, offset: -17, y: 55 },
              { img: 38, rot: 0,  offset: 0,   y: 110 },
              { img: 39, rot: 1,  offset: 17,  y: 165 },
              { img: 40, rot: 3,  offset: 35,  y: 220 },
            ].map((p, i) => (
              <div key={p.img} className="absolute overflow-hidden rounded-2xl border-[4px] border-white shadow-xl"
                style={{
                  width: 200, height: 280,
                  top: `${p.y}px`,
                  left: `calc(50% + ${p.offset}px)`,
                  transform: `translate(-50%, 0) rotate(${p.rot}deg)`,
                  zIndex: i,
                }}
              >
                <img src={`https://i.pravatar.cc/400?img=${p.img}`} alt="" className="h-full w-full object-cover" />
              </div>
            ))}
          </div>
        </div>

        {/* Scroll indicator */}
        <div className="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
          <div className="h-6 w-0.5 rounded-full" style={{ background: "#EC671B" }} />
        </div>
      </div>

      {/* ═══════ Section 2: We Understand ═══════ */}
      <Section id="understand" bg="#FFFFFF">
        <FadeUp>
          <div className="mx-auto max-w-3xl text-center">
            <h2 className="font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
              We Understand Because We Are You
            </h2>
            <p className="mt-4 text-lg leading-relaxed" style={{ color: "#6B655A" }}>
              We&apos;re spiritual seekers ourselves. We know the struggle of finding someone who truly
              understands your path, your practices, and your values. This is why we built this — for
              ourselves and for you.
            </p>
          </div>

          <div className="mt-12 grid gap-6 md:grid-cols-3">
            {[
              {
                icon: Heart,
                title: "The Loneliness of the Path",
                desc: "Walking the spiritual path can feel isolating when those around you don't understand your journey. We know this feeling intimately.",
              },
              {
                icon: HeartHandshake,
                title: "The Need for Deep Connection",
                desc: "You deserve a partner who doesn't just tolerate your practices but celebrates them with you. Someone who truly 'gets it.'",
              },
              {
                icon: Sprout,
                title: "Growth Through Togetherness",
                desc: "When two seekers walk together, the journey becomes richer, more beautiful, and more fulfilling.",
              },
            ].map((card) => {
              const Icon = card.icon;
              return (
              <div
                key={card.title}
                className="rounded-2xl border bg-surface p-8 text-center transition-shadow hover:shadow-lg"
                style={{ borderColor: "#EDE8E0", boxShadow: SHADOW }}
              >
                <div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full" style={{ background: "rgba(236,103,27,0.08)" }}>
                  <Icon className="h-7 w-7" style={{ color: "#EC671B" }} />
                </div>
                <h3 className="mt-5 font-name text-lg font-semibold" style={{ color: "#2D2A24" }}>{card.title}</h3>
                <p className="mt-3 text-sm leading-relaxed" style={{ color: "#6B655A" }}>{card.desc}</p>
              </div>
              );
            })}
          </div>
        </FadeUp>
      </Section>

      {/* ═══════ Section 3: Who We Serve ═══════ */}
      <Section id="serve" bg="#FDFBF9">
        <FadeUp>
          <div className="mx-auto max-w-3xl text-center">
            <h2 className="font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
              A Home for Every Spiritual Seeker
            </h2>
            <p className="mt-4 text-lg leading-relaxed" style={{ color: "#6B655A" }}>
              We serve seekers from all spiritual traditions. Whether you&apos;re an Isha meditator,
              an ISKCON devotee, an Osho follower, an Art of Living practitioner, or walking any other
              path — this is your community. This is your home.
            </p>
          </div>

          <div className="mt-10 flex flex-wrap justify-center gap-3">
            {["Isha Foundation", "ISKCON", "Osho", "Art of Living", "Brahma Kumaris", "Transcendental Meditation", "Yoga Alliance", "Self-Realization Fellowship", "And more..."].map((name) => (
              <span
                key={name}
                className="inline-flex items-center gap-2 rounded-full border px-5 py-2 text-sm font-medium"
                style={{ borderColor: "#EDE8E0", background: "#FFFFFF", color: "#2D2A24" }}
              >
                <span className="h-1.5 w-1.5 rounded-full" style={{ background: "#EC671B" }} />
                {name}
              </span>
            ))}
          </div>

          <div className="mt-10 grid gap-6 md:grid-cols-3">
            {[
              {
                icon: Shield,
                title: "Your Privacy is Sacred",
                desc: "Your privacy is sacred. Even developers cannot access your personal data.",
              },
              {
                icon: CheckCircle,
                title: "Every Profile is Verified",
                desc: "Every profile is verified. Real seekers, real intentions, real trust.",
              },
              {
                icon: Users,
                title: "Built by Seekers, For Seekers",
                desc: "Built by seekers, for seekers. We're in this together.",
              },
            ].map((item) => {
              const Icon = item.icon;
              return (
              <div key={item.title} className="flex flex-col items-center gap-3 rounded-xl p-6 text-center"
                style={{ background: "#FFFFFF" }}>
                <div className="flex h-12 w-12 items-center justify-center rounded-full" style={{ background: "rgba(236,103,27,0.08)" }}>
                  <Icon className="h-6 w-6" style={{ color: "#EC671B" }} />
                </div>
                <h3 className="text-base font-semibold" style={{ color: "#2D2A24" }}>{item.title}</h3>
                <p className="text-sm leading-relaxed" style={{ color: "#6B655A" }}>{item.desc}</p>
              </div>
              );
            })}
          </div>
        </FadeUp>
      </Section>

      {/* ═══════ Section 4: Our Values ═══════ */}
      <Section id="values" bg="#FFFFFF">
        <FadeUp>
          <h2 className="mb-10 text-center font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
            Built on Trust, Privacy &amp; Deep Understanding
          </h2>
          <div className="grid gap-6 md:grid-cols-3">
            {[
              {
                icon: Lock,
                title: "Uncompromising Privacy",
                desc: "Your journey is yours. Your data stays yours. We don't sell, share, or misuse what you trust us with. Period.",
              },
              {
                icon: Compass,
                title: "Deep Understanding",
                desc: "We don't just match profiles. We match practices, values, and spiritual aspirations. Because that's what truly matters.",
              },
              {
                icon: Globe,
                title: "Community First",
                desc: "This isn't a product. It's a community gathering space where meaningful connections naturally bloom. You belong here.",
              },
            ].map((col) => {
              const Icon = col.icon;
              return (
              <div
                key={col.title}
                className="flex flex-col items-center gap-4 rounded-2xl border bg-surface p-8 text-center transition-shadow hover:shadow-lg"
                style={{ borderColor: "#EDE8E0", boxShadow: SHADOW }}
              >
                <div className="flex h-14 w-14 items-center justify-center rounded-full" style={{ background: "rgba(236,103,27,0.08)" }}>
                  <Icon className="h-7 w-7" style={{ color: "#EC671B" }} />
                </div>
                <h3 className="mt-1 font-name text-lg font-semibold" style={{ color: "#2D2A24" }}>{col.title}</h3>
                <p className="text-sm leading-relaxed" style={{ color: "#6B655A" }}>{col.desc}</p>
              </div>
              );
            })}
          </div>
        </FadeUp>
      </Section>

      {/* ═══════ Section 5: The Journey ═══════ */}
      <Section id="journey" bg="#FDFBF9">
        <FadeUp>
          <div className="mx-auto max-w-3xl text-center">
            <h2 className="font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
              Your Journey Starts Here
            </h2>
            <p className="mt-3 text-lg" style={{ color: "#6B655A" }}>
              Three simple steps to finding your community
            </p>
          </div>

          <div className="mt-10 grid gap-8 md:grid-cols-3">
            {[
              {
                icon: Feather,
                title: "Share Your Path",
                desc: "Tell us about your practices, your community, your spiritual aspirations.",
              },
              {
                icon: Search,
                title: "Find Your People",
                desc: "Discover seekers who share your values and truly understand your journey.",
              },
              {
                icon: Sun,
                title: "Grow Together",
                desc: "Build meaningful connections that enrich your spiritual and material life.",
              },
            ].map((step) => {
              const Icon = step.icon;
              return (
              <div key={step.title} className="flex flex-col items-center gap-4 rounded-2xl border bg-surface p-8 text-center"
                style={{ borderColor: "#EDE8E0", boxShadow: SHADOW }}>
                <div className="flex h-14 w-14 items-center justify-center rounded-full" style={{ background: "rgba(236,103,27,0.08)" }}>
                  <Icon className="h-7 w-7" style={{ color: "#EC671B" }} />
                </div>
                <h3 className="font-name text-lg font-semibold" style={{ color: "#2D2A24" }}>{step.title}</h3>
                <p className="max-w-xs text-sm leading-relaxed" style={{ color: "#6B655A" }}>{step.desc}</p>
              </div>
              );
            })}
          </div>
        </FadeUp>
      </Section>

      {/* ═══════ Section 6: Testimonials ═══════ */}
      <Section id="testimonials" bg="#FFFFFF">
        <FadeUp>
          <h2 className="mb-12 text-center font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
            Stories From Our Community
          </h2>
          <div className="grid gap-6 md:grid-cols-3">
            {[
              {
                quote: "I found someone who not only accepts my daily sadhana but practices with me. Isha Connect changed my life. I finally feel seen.",
                author: "Priya",
                tag: "Isha Meditator",
              },
              {
                quote: "After years of feeling alone in my spiritual journey, I found my people here. This is home. I can finally breathe.",
                author: "Arjun",
                tag: "ISKCON Devotee",
              },
              {
                quote: "The privacy and trust this platform offers is unmatched. I felt safe being my true self for the first time.",
                author: "Ananya",
                tag: "Osho Seeker",
              },
            ].map((t) => (
              <div
                key={t.author}
                className="flex flex-col rounded-2xl border bg-surface p-8"
                style={{ borderColor: "#EDE8E0", boxShadow: SHADOW }}
              >
                <p className="flex-1 text-sm italic leading-relaxed" style={{ color: "#6B655A" }}>
                  &ldquo;{t.quote}&rdquo;
                </p>
                <div className="mt-6 flex items-center gap-3">
                  <div className="flex h-10 w-10 items-center justify-center rounded-full text-sm font-semibold text-white"
                    style={{ background: "#EC671B" }}>
                    {t.author[0]}
                  </div>
                  <div>
                    <p className="text-sm font-medium" style={{ color: "#2D2A24" }}>{t.author}</p>
                    <p className="text-xs" style={{ color: "#6B655A" }}>{t.tag}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </FadeUp>
      </Section>

      {/* ═══════ Section 7: Join the Community ═══════ */}
      <Section
        id="join"
        bg="#FDFBF9"
        className="text-center border-t-4"
        style={{ borderTopColor: "rgba(249,168,108,0.5)" }}
      >
        <FadeUp className="flex flex-col items-center gap-6">
          <h2 className="font-name text-3xl font-semibold md:text-4xl" style={{ color: "#2D2A24" }}>
            Your Community Awaits
          </h2>
          <p className="max-w-lg text-lg" style={{ color: "#6B655A" }}>
            Join thousands of spiritual seekers who have found connection, understanding, and companionship.
          </p>
          <button
            type="button"
            onClick={() => router.push("/auth")}
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: "10px",
              borderRadius: "999px",
              padding: "14px 40px",
              fontSize: "16px",
              fontWeight: 600,
              color: "#fff",
              border: "none",
              cursor: "pointer",
              background: "#EC671B",
              transition: "all 0.2s",
            }}
            onMouseEnter={(e) => { (e.target as HTMLButtonElement).style.filter = "brightness(1.1)"; }}
            onMouseLeave={(e) => { (e.target as HTMLButtonElement).style.filter = "none"; }}
          >
            Join Now — It&apos;s Free <ArrowRight style={{ width: "18px", height: "18px" }} />
          </button>
          <p className="text-sm" style={{ color: "#6B655A" }}>
            Complete your profile to connect with like-minded seekers.
          </p>
        </FadeUp>
      </Section>

      {/* ═══════ Section 8: Footer ═══════ */}
      <footer className="px-6 py-12 text-center" style={{ background: "#2D2A24" }}>
        <p className="font-name text-base" style={{ color: "#EDE8E0" }}>
          Isha Connect — A Community for Spiritual Seekers
        </p>
        <p className="mt-3 text-sm" style={{ color: "#6B655A" }}>
          Built by seekers, for seekers
        </p>
        <p className="mt-1 text-xs" style={{ color: "#6B655A" }}>
          Privacy First
        </p>
        <p className="mt-4 text-xs" style={{ color: "#6B655A" }}>
          &copy; {new Date().getFullYear()} Isha Connect
        </p>
      </footer>
    </div>
  );
}
